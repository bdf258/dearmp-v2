version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dearmp.rule=Host(`app.dearmp.uk`)"
      - "traefik.http.routers.dearmp.entrypoints=websecure"
      - "traefik.http.routers.dearmp.tls.certresolver=letsencrypt"
      # Additional security headers via Traefik (if using)
      - "traefik.http.middlewares.security-headers.headers.stsSeconds=31536000"
      - "traefik.http.middlewares.security-headers.headers.stsIncludeSubdomains=true"
      - "traefik.http.middlewares.security-headers.headers.contentTypeNosniff=true"
      - "traefik.http.middlewares.security-headers.headers.frameDeny=true"
      - "traefik.http.routers.dearmp.middlewares=security-headers"

# Optional: If you're using Traefik as a reverse proxy
# networks:
#   default:
#     external:
#       name: traefik
